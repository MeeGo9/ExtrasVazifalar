<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appus.extrasvazifalar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExtrasVazifalar">
        <activity android:name=".fourthtask.NewDetailActivity"></activity>
        <activity android:name=".fourthtask.NewsActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".thirdtask.ContactInfoActivity" />
        <activity android:name=".thirdtask.ThirdTaskActivity" />
        <activity android:name=".secondtask.ColorSecondActivity" />
        <activity android:name=".secondtask.ColorFirstActivity" />
        <activity android:name=".firsttask.SecondActivity" />
        <activity android:name=".firsttask.FirstTask" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>